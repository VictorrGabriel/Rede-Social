/* let nome = document.querySelector("input#nome");
let userName = document.querySelector("input#user");
let email = document.querySelector("input#email");
let dataNascimento = document.querySelector("input#data");
let password = document.querySelector("input#password");
let confirmPassword = document.querySelector("input#cPassword");
let passwordL = document.querySelector("input#passwordL");
let alink = document.querySelector("div#alink");

function isName(nomeR) {
  if (nomeR.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo nome

function isUserN(userN) {
  if (userN.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo nome de usuário

function isEmail(cEmail) {
  if (cEmail.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo email

function isDataNsc(cData) {
  if (cData.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo data de nascimento

function isSenha(cSenha) {
  if (cSenha.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo senha

function isConfSenha(cConfirmSenha) {
  if (cConfirmSenha.value.length == 0) {
    return true;
  } else {
    return false;
  }
} // campo de confirmação de senha

function validar() {
  if (isName(nome)) {
    alert('O campo "Nome" precisa ser prenchido');
  } else {
  }

  if (isUserN(userName)) {
    alert('O campo "Nome de Usuário" precisa ser prenchido');
  } else {
  }
  if (isEmail(email)) {
    alert('O campo "Email" precisa ser prenchido');
  } else {
  }

  if (isDataNsc(dataNascimento)) {
    alert('O campo "Data de Nascimento" precisa ser prenchido');
  } else {
  }

  if (isSenha(password)) {
    alert('O campo "Senha" precisa ser prenchido');
  } else {
   
  }

  if (isConfSenha(confirmPassword)) {
    alert("Confirme a sua senha");
  } else {
  }
}

function validarSenha(entradaPassword) {
    if ((entradaPassword = passwordP)) {
      return true;
    } else {
      return false;
    }
  }


function entrar() {
    if (validarSenha(passwordL)) {
      let link = document.createElement("a");
      link.setAttribute("id", "certo");
      certo.setAttribute("src", "index/index.html");
      alink.appendChild("certo");
    }
  } */
